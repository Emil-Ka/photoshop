import styles from './not-found.module.scss';

const NotFound = () => {
    return (
        <div className={styles.content}>
            Страница не найдена
        </div>
    );
};

export default NotFound;
